In order to build the program, simply run "make" command in terminal. 

To run, the usage is: "./multi-lookup <inputFilePath> <inputFilePath> ... <outputFilePath>". 

To run with valgrind, run:  "valgrind ./multi-lookup <inputFilePath> <inputFilePath> ... <outputFilePath>". 
